# Contributing

Use Python 3.12, install with `pip install -e .`, and run `pytest`, `ruff check .`, and `mypy src` when those tools are available. Keep rules deterministic, evidence redacted, and tools read-only. Do not commit credentials or generated plan files.